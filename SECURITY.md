# Security Policy

Do not commit API keys, personal tokens, `.env` files, private datasets, or
deployment credentials.

If you find a sensitive file or credential in this repository, rotate the
credential first, then open a private security report with the affected path and
commit reference.

The static viewer is designed to run without server-side secrets.
